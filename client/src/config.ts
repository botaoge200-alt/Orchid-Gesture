export const MODEL_CONFIG = {
  // 摄像机位置 [x, y, z]
  CAMERA_POSITION: [0, 0, 40] as [number, number, number],
  
  // 模型缩放比例 [x, y, z]
  MODEL_SCALE: [19, 19, 19] as [number, number, number],
  
  // 模型中心偏移量 [x, y, z] (用于调整垂直位置)
  MODEL_OFFSET: [0, 10.5, 0] as [number, number, number],
  
  // 视野角度
  FOV: 45
}
